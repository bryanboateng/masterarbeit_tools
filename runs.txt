# One Weights & Biases run URL per line, in any order. Everything after a "#"
# is ignored. The page generator reads the dataset percentage, the method and
# the evaluation condition from each run itself, so a URL is all it needs:
#
#   uv run create_results_page.py
#
# The sections below are only a checklist. A run pasted under the wrong one
# still lands in the right cell.

# --- grippy undisturbed ---
# https://wandb.ai/bryanboateng-team/2026-08-18-10-tacil-dp-bounded-grippy_undisturbed/runs/3ohsiome

# --- grippy low disturbance ---

# --- grippy high disturbance ---

# --- slippery undisturbed ---

# --- slippery low disturbance ---

# --- slippery high disturbance ---
